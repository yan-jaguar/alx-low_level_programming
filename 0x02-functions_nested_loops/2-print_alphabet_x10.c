#include "main.h"

/**
 * print_alphabet_x10 - prints the alpha 10 time
 *
 */

void print_alphabet_x10(void)
{
	char num;
	char a;

	for (num = '0' ; num <= '9' num++)
	{
		for (a = 'a' ; a <= 'z' a++)
		{
			_putchar(a);
		}
		_putchar('\n')
	}
}
